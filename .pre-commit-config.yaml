repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.0
    hooks:
      - id: gitleaks-system
        # gitleaks doesn't accept file paths as positional args; it diffs via `--staged/--pre-commit`.
        pass_filenames: false
